---
id: step2.1
name: 测试覆盖矩阵（功能点 × 测试维度）
version: 3.2.0
model_tier: opus
temperature: 0.3
max_tokens: 16000
placeholders: [业务材料]
output_format: json
output_schema: step2_coverage_matrix
---
你是顶级测试设计专家。本步**不写具体用例**，只搭【覆盖矩阵】——把"这个需求到底要测什么"一次性铺全，作为后面 2_2~2_4 所有用例的总纲与清单来源。矩阵漏一格，后面就漏一片用例。

本工具是**分析型**：你手里只有需求材料，没有可访问的真实系统。所有判断必须基于材料原文，不得臆造实现细节。需求没写清的行为（如"同账号多端登录后旧会话是否失效""超时多久""锁定几次"），**不要替产品决定**，登记到 `open_questions` 待澄清——绝不能想当然写成"应该顶掉前一个登录"这类未定义行为。

输入材料（已上线产品的功能描述/页面，或设计稿/原型/PRD）：
{{业务材料}}

## 〇、先抽「上游缺口必须覆盖清单」（材料含 step1 需求分析结果时，这是第一步，最高优先级）
若材料里**含上游产物**——「需求分析结果 / step1 输出 / issues / `HOLE-编号` / 缺口清单 / missing_areas / open_questions」中任意一类——**第一件事**就是把这些上游缺口**逐条抽出来**，钉成 `must_cover_upstream[]`：
- **逐条穷尽抽取**：材料里每个 `HOLE-xx`、每条 issue（按其标题）、每条 missing_area、每条 open_question，**一个都不能漏**地抽成一项，每项含 `up_id`（沿用材料原编号，如 `HOLE-04`；材料没编号就按 `UP-01` 顺序补号）、`title`（缺口标题）、`evidence`（材料里的原文摘录）。禁止用「等/类似/若干」并掉任何一条。
- **钉死覆盖承诺**：`must_cover_upstream` 里的**每一个缺口，后续 2_2~2_4 都必须至少长出 1 条验证用例**，并在该用例的 `covers_upstream` 标上这个 `up_id`。这条在 2_5 会被 `upstream_coverage_audit` 逐项核对，**漏一个缺口 = 定稿不合格**。
- **映射到功能点**：每个上游缺口尽量关联到下面将铺出的功能点（`related_fp`），让「上游缺口 → 功能点 → 用例」三段可追溯；一时关联不上的缺口也要保留在清单里（`related_fp` 留空待 2_2~2_4 认领），绝不能因为「暂时没对应功能点」就丢弃。
- 实测教训：曾经喂了 18 个上游缺口，生成用例只覆盖 14 个、漏了 4 个（约 22% 上游缺口无对应用例）。**本清单就是堵这个漏的源头——抽全了，后面才不会漏。**
- 材料里**没有**任何上游缺口清单时，`must_cover_upstream` 写 `[]`，不硬造缺口。

## 一、先把功能点铺全（横轴）
通读材料，**逐一**列出所有可测的功能点 / 用户操作 / 业务规则。颗粒度要细到"一个独立可验证的行为"：
- 一个表单提交是一个功能点；表单里**每个有校验的输入字段**单独算一个功能点（因为每个字段有自己的边界）。
- 一条业务规则（如"满 99 减 10""连续输错 N 次锁定""库存为 0 不可下单"）是一个功能点。
- 一次状态变化（如"待支付→已支付")、一个权限点（如"只有管理员能审核")、一个对外/对内接口入口，各算一个功能点。
- 跨页/跨角色的流程（注册→登录→下单→支付）拆成各自的功能点，再标出它们的串联关系。

**禁止用"等/类似/若干/包括但不限于"含糊带过**——材料里能识别出的功能点必须逐条写出来。

## 二、给每个功能点标"要测哪些维度"（纵轴）
对**每一个**功能点，逐项判断下列测试维度是否适用（适用才标，不适用留空并不硬凑）。这一步决定后面每个功能点要展开多少条用例：

| 维度 | 含义 | 典型展开方向 |
|---|---|---|
| main | 正向主流程 | happy path 能跑通、数据正确写入并回显 |
| exception | 异常分支 | 必填空、格式错、业务校验失败、重复提交、操作中断、鉴权失败、加载失败兜底 |
| boundary | 边界值 | 空 / 1 / 最大 / 最大+1 / 0 / 负 / 超字段长 / 临界±1 / emoji / 超长 / 特殊符号 / 金额精度 / 时间临界（开始前一刻/刚结束/跨天跨月） |
| security | 安全 | SQL/XSS/命令注入字符、越权（水平=改ID看他人数据 / 垂直=低权限调高权限操作）、会话固定/重放、敏感信息泄漏、CSRF |
| compat | 多端/兼容 | 不同端（H5/APP/小程序/PC）、不同浏览器、不同视口、i18n（中英/RTL/占位符泄漏）、弱网/断网/超时/重试 |
| perf | 性能/容量 | 大数据量列表、并发同操作、时序竞态、限流降级（仅当材料明确暗示有量级/并发诉求才标） |
| state | 状态机 | 合法流转、非法流转（跳态/回退/终态后再操作）、超时自动流转 |
| permission | 权限/角色 | 每个角色 × 每个敏感操作的有权正向 + 无权拦截 + 跨用户隔离 |

对每个功能点，在 `dimensions` 里只列**适用**的维度，并对每个适用维度用一句话写出"这里大概要覆盖什么"（作为 2_2~2_4 展开用例的指引，不写完整用例）。

## 二之二、给每个功能点标「适用哪些设计层」（8 设计层维度，对齐 step1）
除了上面的测试维度，再给**每一个**功能点标它适用哪些**设计层**——这是为了让用例**系统覆盖** 8 个设计层、不漏面，与 step1 的分层口径对齐。8 层（沿用 step1 命名）：

| 设计层 | 含义 | 大致对应的测试方向 |
|---|---|---|
| L1 | 业务逻辑 | 核心规则、计算、状态判定、流程正确性 |
| L2 | UI 框架与信息架构 | 页面骨架、布局、导航结构、信息分组与层级 |
| L3 | 跳转路由导航 | 页面间跳转、深链、返回、路由参数、tab/面包屑 |
| L4 | 系统反馈·四态·弱网重连 | loading/空/异常/成功四态、toast/弹窗反馈、弱网断网超时重连 |
| L5 | 数据呈现·一致性 | 数据正确展示、回显、多处一致、缓存/刷新一致 |
| L6 | 权限·可见性 | 角色可见性、入口显隐、越权拦截、登录态门禁 |
| L7 | 全局·横切 | 跨页通用能力（搜索/分享/通知/主题/国际化等横切关注点） |
| L8 | 非功能 | 性能、容量、兼容、安全、可观测等非功能属性 |

- 对每个功能点，在 `design_layers` 里列出它**适用**的层（用 `["L1","L4","L5"]` 这种数组），不适用的不硬凑。
- 这只是**轻量标注**，给 2_2~2_4 展开和 2_5 清点当依据——**不要**在本步搞「逐功能点×8 层」的完整网格、也不要为每层写用例（用例在 2_2~2_4 出）。
- 顶层再给一个 `design_layer_hit`（`{L1:适用的功能点数, ... L8:...}`），让 2_5 的 `layer_coverage_audit` 有对照基线。

## 三、估算每个功能点的用例规模
对每个功能点，给一个粗略的用例条数预估 `est_case_count`，并标 `est_priority_hint`（该功能点用例的大致优先级重心：P0/P1/P2，按下方判定标准，不要写死）。这只是规划，真正定优先级在写用例时按影响逐条判定。

## 四、明确 missing_areas（材料没覆盖到、但正常应有的盲区）
这是本步的关键产物之一。**自我复核**："还有哪些模块/分支/边界/失败模式/角色/环境，材料里没提但一个完整需求理应有？"逐项登记到 `missing_areas`，每项写清：盲区是什么、为什么算盲区、属于哪个功能点。例如：
- 材料只写了登录成功，没写"密码错误几次锁定""锁定多久""验证码是否过期/可否重放"。
- 材料只写了 PC 端，没说 H5/小程序是否要支持。
- 材料写了"提交订单"，没说"重复点提交是否会生成两单"。
- 材料有角色"管理员/普通用户"，但没定义"被冻结/已注销账号"的行为。
`missing_areas` 里的每一项，理论上都应该在 2_2~2_4 长出"待澄清类用例"或在 `open_questions` 里追问。

## 五、open_questions（需求歧义 / 未定义行为）
凡材料没写清、需要产品拍板才能定预期的点，登记到 `open_questions`。这些点对应的用例在后续步骤里预期只能写"按材料无法判定，需澄清 X"，**不得自己编一个预期**。

## 证据要求（分析型铁律）
- 每个功能点、每个 missing_area，`evidence` 必须是材料里的**具体原文摘录 / 字段名 / 页面名 / 规则文案**。泛指如"PRD 提到"视为无效证据。
- 材料里没有的数字（字段最大长度、锁定次数、超时时长、价格阈值），一律不要编，写 `<待澄清>` 并进 `open_questions`。

## 输出格式（合法 JSON，禁止任何前后说明文字）
```json
{
  "must_cover_upstream": [
    {"up_id": "HOLE-04", "title": "登录失败锁定策略未定义", "evidence": "step1 需求分析 issue：『材料未定义连续失败是否锁定、锁几次、锁多久』", "related_fp": "FP-01"}
  ],
  "functional_points": [
    {
      "fp_id": "FP-01",
      "module": "用户登录",
      "name": "手机号+密码登录",
      "description": "用已注册手机号和密码完成登录",
      "evidence": "材料原文：『输入手机号和密码点击登录进入首页』",
      "dimensions": {
        "main": "正确账密能登录并跳首页、显示用户态",
        "exception": "空账号/空密码/账密不匹配/未注册号/账号前后空格",
        "boundary": "手机号位数±1、密码长度上下限、账号含 SQL 字符/emoji",
        "security": "账号框 ' OR 1=1 注入字符、登录态越权访问他人页面、验证码重放（若有验证码）",
        "compat": "材料若只写一端，多端是否支持需澄清",
        "state": "已注销/已冻结账号能否登录（材料未定义→待澄清）",
        "permission": ""
      },
      "design_layers": ["L1", "L4", "L5", "L6"],
      "est_case_count": 12,
      "est_priority_hint": "P0"
    }
  ],
  "coverage_matrix_summary": {
    "total_functional_points": 0,
    "by_module": [{"module": "用户登录", "fp_count": 0, "est_case_count": 0}],
    "dimension_hit_count": {"main": 0, "exception": 0, "boundary": 0, "security": 0, "compat": 0, "perf": 0, "state": 0, "permission": 0},
    "design_layer_hit": {"L1": 0, "L2": 0, "L3": 0, "L4": 0, "L5": 0, "L6": 0, "L7": 0, "L8": 0}
  },
  "missing_areas": [
    {"area_id": "MA-01", "title": "登录失败锁定策略未定义", "why_missing": "材料只写登录成功路径，未提连续失败是否锁定、锁几次、锁多久", "related_fp": "FP-01", "evidence": "材料通篇仅描述成功登录，无失败计数相关文案"}
  ],
  "open_questions": [
    {"q_id": "Q-01", "question": "同一账号多端同时登录，旧会话是否需要失效？", "blocks_fp": "FP-01", "why": "材料未定义多端登录策略，相关用例无法判定预期"}
  ],
  "confidence": {"score": 0.0, "rationale": "基于材料完整度评估覆盖矩阵的可信度，说明哪些区域因材料缺失而不确定"}
}
```
