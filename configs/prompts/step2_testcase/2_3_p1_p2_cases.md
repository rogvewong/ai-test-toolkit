---
id: step2.3
name: 异常分支 + 边界值用例
version: 3.2.0
model_tier: opus
temperature: 0.3
max_tokens: 16000
placeholders: [业务材料]
output_format: json
output_schema: step2_exception_boundary_cases
---
你是顶级测试设计专家，正在为【人工执行】的测试团队设计用例。本步是整条流水线**最重要、最考验深度**的一步：把每个功能点的**异常分支**和**输入边界**穷尽。测试是细节中的细节——这一步漏一条，线上就多一个坑。

本工具是**分析型**：只有需求材料，没有真实系统可点。用例是设计物，`status` 一律 `designed`，`automation_tag` 一律 `manual`。

输入材料：
{{业务材料}}

## 从覆盖矩阵接力 + 穷尽展开
2_1 已标出每个功能点适用的 `exception` / `boundary` 维度。本步**对每个功能点逐项展开**，**禁止用"等/类似/若干/包括但不限于"含糊带过**——适用的场景必须逐条写成用例。

**上游缺口关联**：2_1 的 `must_cover_upstream` 里凡属于**异常/边界性质**的上游缺口（如「失败锁定策略未定义」「超长输入处理未定义」），本步**必须**长出对应用例并在 `covers_upstream` 标上 `up_id`（如 `["HOLE-04"]`）——这些缺口在 2_5 会被逐条审计，漏一个即定稿不合格。无对应上游缺口的用例写 `covers_upstream: []`。

### A. 异常分支（exception）——对每个输入、每个分支
逐条枚举（适用才写）：
- **必填校验**：每个必填项**各自**空着提交一条。
- **格式校验**：每个有格式要求的字段（手机号/邮箱/身份证/金额）填非法格式各一条。
- **业务校验失败**：不合法的值、当前状态不允许的操作、超出额度/库存。
- **重复 / 幂等**：连续快速点两次提交、表单重复提交——写操作必须有"重复操作不重复生效（不生成两单/不重复扣款）"用例。
- **操作中断**：中途取消、返回上一步再进来、刷新页面后继续。
- **鉴权失败**：未登录就操作、登录态过期后操作、权限不足（垂直越权细化留 2_4，这里出"态过期/未登录"基础兜底）。
- **兜底 / 失败态**：列表数据为空时的空态、加载失败时的提示、网络异常的提示。
- **并发 / 时序**（仅当材料暗示有并发/库存/余额诉求）：两端同时操作同一资源。

### B. 边界值（boundary）——对每个输入的边界
对每个有取值范围的输入，逐条覆盖：
- **数值**：最小值 / 最小值−1 / 最大值 / 最大值+1 / 0 / 负数。
- **文本长度**：空 / 1 字 / 中间长度 / 最大长度 / 最大长度+1（超字段长）。
- **字符类型**：纯中文 / 纯英文 / 纯数字 / emoji / 特殊符号 / 前后空格 / 全空格。
- **金额精度**：最小币种单位 / 多位小数 / 0 元 / 负数 / 超额度。
- **数量**：0 条 / 1 条 / 上限 / 上限+1。
- **时间**：活动开始前一刻 / 刚开始 / 刚结束 / 结束后一刻 / 跨天 / 跨月 / 跨年。
- 每条用例聚焦**一个**具体边界值。

### 深度标杆（必须达到这个穷尽度）
以"登录"为例，**应当**长出（材料涉及到才出对应条）：空账号 / 空密码 / 账号前后空格 / 大小写敏感性 / 超长账号(>字段长) / 账号含 SQL 字符 `' OR 1=1`（注入归 security，放 2_4） / emoji 账号 / 密码连错 1→N 次触发锁定 / 锁定时长边界 / 锁定期间用正确密码 / 解锁后重试 / 验证码过期 / 验证码重放 / 未注册号登录 / 已注销账号登录 / 已冻结账号登录 / 首次登录强制改密 / 密码到期 …… 每条带前置+步骤+单一可断言预期+判定依据。**其它功能点要拿出同等穷尽度。**

## 分域标准测试点清单（Domain Checklist）—— 系统性兜底，防漏域（本步必跑）

上面 A/B 是「自由穷尽」，本节是「按业务域逐条对表设计用例」，专治「临场想不到、系统性漏掉」（实测漏过：套餐升级降级差价、并发设备数/防账号共享、风控盗刷）。这是一份**覆盖常见产品功能全集的通用业务域库**（共 17 个域：账号/支付/订阅/内容媒体/搜索/列表/文件/表单/社交/消息/权限/营销/流程/隐私/国际化/三方集成/通用质量），无论测的是会员订阅、搜索、上传、IM、直播还是社交，都先用它扫一遍防漏域。**这份域库与第 1 步（需求评审）的 1_4、以及本工具 2_4 共用同一份，口径必须一致。**

用法（强制流程）：
1. **先判定材料涉及哪些域**（17 个域逐个判，只要有一句话/一个字段/一个按钮指向该域功能即算「涉及」），在 `domain_coverage[].applicable` 记 `true/false`。不涉及的域整体记 `false` 即可，不必逐点。
2. **对每个涉及的域，把下面属于「异常/边界性质」的点逐条设计成可执行用例**——凡适用必列尽，禁止用「等/类似/若干」带过。每个点至少一条用例，确实需要多条（如「支付结果分支」有 7 种结果）就拆成多条，每条聚焦一种。
3. **分工边界**（共用同一清单、按性质分到不同步骤，避免重复也避免遗漏）：
   - **本步 2_3 负责**：清单里属于**异常分支 / 输入边界 / 幂等去重 / 金额精度 / 时序并发**性质的点（下面已用 ⟦2_3⟧ 标注）。
   - **2_4 负责**：清单里属于**权限/越权/安全/状态机非法流转**性质的点（下面已用 ⟦2_4⟧ 标注）；本步**不重复**这些，但若某点同时跨异常与状态机（如「订单超时自动关单」），本步出「超时未支付后再点支付被拒」的异常用例，状态机流转那一跳留 2_4。
4. **PRD 没定义该点怎么处理时**：用例照常设计（步骤照常写），但 `expected` 写「材料未规定 X 的处理方式，需澄清」、`ambiguity` 标 `true`，并确保该歧义已登记进 2_1 `open_questions`（`remark` 注明对应 Q 编号）。**绝不替产品二选一编预期。**
5. **这是最低覆盖基线，不是上限**：清单是地板不是天花板。材料涉及但本清单没列到的子点/子域，**自行类比展开**补设计用例，在 `domain_coverage[].extra_cases_added` 说明补了哪些。

> 域库共 17 个域（D-ACCT / D-PAY / D-SUB / D-CONTENT / D-SEARCH / D-LIST / D-FILE / D-FORM / D-SOCIAL / D-MSG / D-AUTHZ / D-MKT / D-FLOW / D-PRIVACY / D-I18N / D-INTEG / D-GEN）。每个点已标 ⟦2_3⟧（本步出）或 ⟦2_4⟧（留 2_4）。高危域（D-PAY/D-SUB/D-AUTHZ）给了全量点，其余域给的是「最易漏的关键点」——不是上限，涉及就按同口径自行类比补全。

### D-ACCT · 账号与身份域（材料涉及注册、登录、账号体系任一即适用）
逐点设计用例：
- ⟦2_3⟧ **注册**：账密/手机/邮箱/三方/扫码各路径走通各一条、已被占用的手机号或邮箱再注册一条、注册必填项各空一条、密码强度不达标一条。
- ⟦2_3⟧ **登录**：账密登录成功一条、密码错误一条、验证码登录一条、验证码过期一条、验证码重放一条、记住我 token 过期后再访问一条、密码连错 1→N 次触发锁定一条、锁定时长边界一条、锁定期间用正确密码一条。
- ⟦2_3⟧ **登出/登录态**：主动登出后旧 token 失效一条、登录态到期后操作被要求重登一条。
- ⟦2_3⟧ **找回/改密**：找回密码走通一条、改密后是否要求全端重登一条。
- ⟦2_3⟧ **账号状态**：已注销账号登录一条、已冻结账号登录一条、已封禁账号登录一条、解封后登录一条（PRD 未定义各状态行为则标待澄清）。
- ⟦2_4⟧ **多端顶号 / 异地登录 / 实名 / 越权**：多端同时登录顶号策略、异地新设备二次校验、实名信息越权、登录态越权访问他人资源——留 2_4（属权限/安全/状态机性质）。

### D-PAY · 支付 / 交易域（材料涉及下单、付款、收银台、交易、退款、对账任一即适用）
逐点设计用例（标 ⟦2_3⟧ 的本步出，⟦2_4⟧ 的留 2_4）：
- ⟦2_3⟧ **下单**：重复下单各一条、并发下单（同用户/同商品同时多次提交）一条、同一商品存在多笔未支付订单一条、跨端下单（H5 下单 APP 继续付）一条。
- ⟦2_3⟧ **支付结果分支**：成功 / 失败 / 用户取消 / 超时未结 / 关单后到账 / 部分支付 / 支付中关闭页面——**每种结果各一条**，断言订单状态与用户所见。
- ⟦2_3⟧ **幂等**：支付回调重复通知不重复开通一条、同订单重复开通权益只发一次一条、重复点击不重复扣款一条（用例点明幂等键=订单号+交易号）。
- ⟦2_3⟧ **回调**：异步回调丢失/延迟/乱序后查单兜底一条、主动查单与回调状态冲突时以谁为准一条（伪造回调验签归 2_4 安全）。
- ⟦2_3⟧ **金额**：0 元一条、负数一条、超大额一条、分/元精度与四舍五入一条、优惠后金额计算与展示一条（**篡改金额**归 2_4 越权/安全）。
- ⟦2_3⟧ **对账**：支付平台与本地状态长时间不一致一条、掉单一条、长时间未回调一条（断言系统有对账/告警/补偿，PRD 没定义则标待澄清）。
- ⟦2_3⟧ **退款**：全额退款一条、部分退款一条、退款后权益回收与到期时间调整一条、退款幂等（不重复退）一条、超时退款一条、已消费部分如何算一条。
- ⟦2_3⟧ **价格**：下单锁价快照一条、改价对「未下单 / 已下单未付 / 支付中」分别的影响各一条、展示价与下单价不一致以谁为准一条。
- ⟦2_4⟧ **风控**：盗刷、异常交易频次、同卡多账号/同账号多卡、黑产批量、风控拦截后体验——留 2_4 安全。
- ⟦2_4⟧ **订单状态机**：各状态非法跳转、各状态允许的操作、超时自动关单的状态流转——留 2_4 状态机（本步只出「超时后再操作被拒」这类异常面）。

### D-SUB · 订阅 / 会员 / 计费域（材料涉及会员、套餐、订阅、续费、权益、有效期任一即适用）
逐点设计用例：
- ⟦2_3⟧ **套餐变更**：升级一条、降级一条、差价补/退各一条、不同时长套餐时长换算一条、跨档变更生效时点（立即/下周期）一条。
- ⟦2_3⟧ **续费**：有效期内续费时长叠加（原到期日顺延 vs 从今天重算）一条、到期后重购一条、连续包月周期一条。
- ⟦2_3⟧ **自动续费**：是否自动扣款一条、解约一条、扣款失败重试一条、到期前扣款时点一条（产品有自动续费才出）。
- ⟦2_3⟧ **到期**：到期判定精确到时刻+时区一条、到期当天可用性一条、宽限期一条、降级/失权触发时机与定时任务时点一条。
- ⟦2_4⟧ **并发**：同一会员多设备同时在线数上限、防账号共享、异地同时登录、顶号策略——留 2_4（属并发/权限性质，本步只出「超出设备数时新登录的提示」这类用户可见异常面，顶号的会话失效逻辑留 2_4）。
- ⟦2_3⟧ **权益边界**：免费 vs 专属边界一条、清晰度权限矩阵（游客/注册未付费/会员 × 免费/专属）逐格、试看片段一条、全端权益通用与登录态跨端同步一条。
- ⟦2_3⟧ **提醒**：到期前提醒时点/渠道/去重/触发次数各覆盖一条。

### D-CONTENT · 内容与媒体域（材料涉及视频、图文、音频、直播任一即适用）
逐点设计用例：
- ⟦2_3⟧ **视频播放**：起播一条、清晰度切换一条、倍速一条、续播记忆一条、弱网缓冲/卡顿兜底一条、字幕开关一条、横竖屏切换一条、版权下架后已缓存内容的表现一条。
- ⟦2_3⟧ **图文/音频**：加载失败兜底一条、长内容分段/进度一条、音频后台/锁屏播放一条（音频涉及时）。
- ⟦2_3⟧ **直播+弹幕**：断流重连一条、弹幕发送一条、弹幕敏感词过滤一条（直播涉及时）。
- ⟦2_3⟧ **下载/缓存**：缓存过期清理一条、离线播放一条（防盗链/播放地址鉴权归 2_4）。
- ⟦2_3⟧ **内容审核**：举报入口一条、违规下架后对已访问用户的提示一条（审核越权/绕过归 2_4）。

### D-SEARCH · 搜索 / 筛选 / 排序 / 推荐域（材料涉及搜索框、筛选项、排序、推荐位任一即适用）
逐点设计用例：
- ⟦2_3⟧ **关键词**：联想一条、拼写纠错一条、同义词一条、空关键词一条、纯空格一条、特殊字符一条、超长关键词一条。
- ⟦2_3⟧ **空结果**：无匹配结果空态兜底一条。
- ⟦2_3⟧ **敏感词**：搜索敏感词被过滤/提示一条。
- ⟦2_3⟧ **筛选**：多条件组合一条、互斥条件冲突一条、筛选后无结果一条、重置筛选一条。
- ⟦2_3⟧ **排序**：各排序维度各一条、排序与筛选叠加一条。
- ⟦2_3⟧ **分页/滚动**：无限滚动加载一条、翻页时数据变化导致重复/遗漏一条。
- ⟦2_3⟧ **推荐**：推荐去重一条、冷启动无行为数据兜底一条（有推荐时）。

### D-LIST · 列表 / Feed / 分页域（材料涉及任何列表、信息流、分页展示即适用）
逐点设计用例：
- ⟦2_3⟧ **加载态**：空态一条、首屏加载一条、加载失败重试一条。
- ⟦2_3⟧ **刷新/加载更多**：下拉刷新一条、上拉加载更多一条、到底提示一条。
- ⟦2_3⟧ **去重/置顶**：列表去重一条、置顶项一条、刷新后顺序稳定一条。
- ⟦2_3⟧ **大数据量**：极长列表/大分页性能一条（数据量可能很大时）。

### D-FILE · 上传 / 下载 / 文件域（材料涉及上传、附件、导入导出、文件下载任一即适用）
逐点设计用例：
- ⟦2_3⟧ **限制**：不支持的格式一条、超大小上限一条、超数量上限一条、超分辨率/时长一条（每条断言提示）。
- ⟦2_3⟧ **传输**：断点续传/秒传一条、上传中弱网中断后恢复一条、失败重传一条、上传进度一条。
- ⟦2_3⟧ **预览**：上传后预览一条。
- ⟦2_4⟧ **安全/权限**：安全合规扫描（木马/违规）、私有文件凭直链被他人下载——留 2_4 安全。

### D-FORM · 表单与 CRUD 域（材料涉及表单填写、增删改查、批量、导入导出任一即适用）
逐点设计用例：
- ⟦2_3⟧ **校验**：必填项各空一条、格式校验各一条、字段联动校验一条。
- ⟦2_3⟧ **草稿**：草稿/自动保存一条、中途退出再回来一条（长表单时）。
- ⟦2_3⟧ **重复提交**：连续快速提交不重复生效一条。
- ⟦2_3⟧ **批量/导入导出**：批量操作部分失败处理一条、导入模板校验与错误回执一条、导出范围一条。
- ⟦2_3⟧ **删除**：删除二次确认一条、软删除/回收站/恢复一条（删除权限归 2_4）。

### D-SOCIAL · 社交互动域（材料涉及评论、点赞、关注、分享、@、举报任一即适用）
逐点设计用例：
- ⟦2_3⟧ **评论**：评论/回复/盖楼各一条、评论敏感词过滤一条、空评论/超长评论各一条。
- ⟦2_3⟧ **互动关系**：点赞与取消幂等一条、收藏一条、关注/取消关注一条、重复点赞不重复计数一条。
- ⟦2_3⟧ **分享**：站内分享一条、站外分享一条、生成海报一条、分享的内容已删除/已变更时的表现一条。
- ⟦2_3⟧ **举报**：举报入口与提交一条。
- ⟦2_4⟧ **拉黑/屏蔽 越权**：拉黑后双方可见性变化、删除他人评论的权限——留 2_4 权限。

### D-MSG · 消息与通知域（材料涉及 IM、私信、推送、站内信、红点任一即适用）
逐点设计用例：
- ⟦2_3⟧ **IM**：发送一条、撤回一条、已读未读一条、离线消息上线后收取一条、多端同步一条、消息顺序/去重一条（有即时通讯时）。
- ⟦2_3⟧ **推送**：推送到达一条、点击跳转一条、免打扰时段一条、角标计数一条。
- ⟦2_3⟧ **红点/未读**：未读计数口径一条、清除时机一条、多端一致一条。
- ⟦2_3⟧ **订阅偏好**：退订某类通知后不再收到一条。

### D-AUTHZ · 权限 / 角色 / 越权域
⟦2_4⟧ 整域留 2_4（垂直越权、水平越权、直链绕过、鉴权 token）。**本步不出**，仅在此列出以保持清单完整、便于核对。

### D-MKT · 营销与增长域（材料涉及优惠券、活动、积分、签到、邀请、抽奖、排行榜任一即适用）
逐点设计用例：
- ⟦2_3⟧ **优惠券**：领取限制一条、叠加规则一条、不满门槛使用一条、过期券使用一条、与其它优惠互斥一条、退款后券返还一条。
- ⟦2_3⟧ **活动**：库存售罄一条、超限购一条、活动开始前/结束后入口表现各一条。
- ⟦2_3⟧ **积分/签到**：积分获取与消耗一条、签到中断/补签/跨天判定一条。
- ⟦2_3⟧ **邀请/抽奖**：邀请奖励发放时机一条、奖励上限一条、抽奖奖品库存并发一条（防自邀/防刷归 2_4）。
- ⟦2_4⟧ **防刷/概率合规**：同人同设备同 IP 防刷、抽奖概率合规披露——留 2_4 安全。

### D-FLOW · 流程 / 审核 / 工单域（材料涉及多步流程、审批、工单、状态流转任一即适用）
逐点设计用例：
- ⟦2_3⟧ **多步流程**：向导回退一条、中途退出数据暂存一条。
- ⟦2_3⟧ **审批流**：正常审批通过一条、驳回一条、撤回一条、超时自动流转一条（`remark` 标调小超时触发）。
- ⟦2_3⟧ **工单**：工单创建/流转/关闭各一条、重新打开一条（涉及工单时）。
- ⟦2_4⟧ **并发审批/状态机非法**：同一单多人同时审批冲突、非法状态跳转、节点可操作人越权——留 2_4。

### D-PRIVACY · 数据 / 隐私 / 合规域（材料涉及个人信息收集、数据导出删除、未成年人、隐私协议任一即适用）
逐点设计用例（材料未提出要求的标待澄清，不臆造）：
- ⟦2_3⟧ **收集授权**：拒绝授权后的降级体验一条。
- ⟦2_3⟧ **数据导出/删除**：导出个人数据一条、注销时数据带走/清除一条。
- ⟦2_3⟧ **未成年人**：青少年模式开启后内容受限一条（可能涉及未成年用户时）。
- ⟦2_4⟧ **脱敏/协议合规**：敏感信息展示脱敏、隐私协议变更后二次同意——留 2_4 安全/合规。

### D-I18N · 国际化 / 本地化域（仅当材料暗示需多语言、多地区、多币种、多时区时适用）
逐点设计用例：
- ⟦2_3⟧ **多语言**：中英切换一条、超长译文不溢出一条、占位符不泄漏一条、RTL 布局一条。
- ⟦2_3⟧ **时区**：跨时区时间显示一条、夏令时一条。
- ⟦2_3⟧ **币种**：多币种金额格式一条（涉及多币种时）。

### D-INTEG · 第三方集成域（材料涉及三方登录/支付/分享/地图/客服/推送等外部 SDK 或服务即适用）
逐点设计用例：
- ⟦2_3⟧ **失败降级**：三方对接失败一条、超时一条、不可用时降级提示一条。
- ⟦2_3⟧ **回调兜底**：三方回调丢失/重复的兜底一条。
- ⟦2_3⟧ **配额限流**：三方配额/限流触顶时的处理一条（有配额约束时）。
- ⟦2_4⟧ **回调验签**：三方回调验签——留 2_4 安全。

### D-GEN · 通用质量（所有功能都要对一遍，不分域）
逐点设计用例：
- ⟦2_3⟧ **输入**：空 / 超长(超 DB 字段) / 特殊字符 / emoji / 前后空格 / 大小写敏感 各代表性一条（**SQL-XSS-命令注入字符**归 2_4 安全）。
- ⟦2_3⟧ **并发与时序**：竞态一条、重复提交一条（状态机非法跳转留 2_4）。
- ⟦2_3⟧ **网络**：弱网一条、断网一条、超时一条、重试幂等一条（提示与恢复行为）。
- ⟦2_3⟧ **降级容错**：依赖不可用时的降级/容错表现一条。
- ⟦2_3⟧ **数据一致性**：缓存失效/更新时机一条、多处数据一致一条。
- ⟦2_4 / 2_5⟧ **可观测**：关键操作埋点、错误日志——属信息性核查，用例性弱，归 2_4 或在 2_5 作为 issue/待澄清登记，本步不强凑。

## 每条用例的硬要求
- `preconditions`：精确前置，含要构造的数据/账号/初始状态。
- `steps`：自然语言带序号操作数组，**重点写清"怎么制造这个异常/边界"**——在哪一步、哪个输入框、填入什么具体值、做什么操作。禁止结构对象 / 接口 / 代码。
- `expected`：**单一可断言**的现象（出现什么具体文案 / 跳到哪 / 按钮变什么样 / 数据没被改）。禁止"显示正常"。
- **超时 / 锁定时长 / 自动过期类**用例：必须在 `remark` 标注「需配合调小超时配置触发，非真实干等 N 分钟」，并写出触发方式。

## 边界预期的防幻觉（关键）
- 边界的预期**不允许写"接受或拦截都算对""提示 A 或 B 都行"**。需求规定了就照规定写死预期；**需求没规定**这个边界怎么处理，就**不要二选一**——把 `expected` 写成「材料未规定 X 边界的处理方式」，把 `ambiguity` 标 true，并确保这条歧义已登记进 2_1 `open_questions`（在 `remark` 注明对应 Q 编号）。
- 材料里没有的阈值（最大长度、锁定次数、超时分钟、金额上限），**绝不编造**。用例可以写"输入超过【字段最大长度】的字符串"，把具体数字留作 `<待澄清>`，不要硬填一个 21、255。
- 凡材料未定义的行为，不臆造系统反应。

## 优先级（按影响逐条判定，不要写死"异常=P1 / 边界=P2"）
判定标准：**P0**=破了导致主流程不可用 / 资金/订单/库存/数据正确性受损（如"重复提交生成两单""超额下单不拦截"）→ 即便是异常/边界也给 **P0**；**P1**=重要功能受损但有绕行、关键校验缺失；**P2**=次要/边缘体验；**P3**=纯提示性（文案微差）。**异常不等于 P1、边界不等于 P2**——按这条用例真实的业务影响给。
- 涉及金额 / 库存 / 积分的异常，必须有"不超扣 / 不少扣 / 不超卖"用例，优先级按资损影响通常 P0。
- 用例数克制：按模块聚合，避免机械组合灌水；同一字段的同类边界不必把每种特殊符号都列满，挑有代表性的（emoji / 空格 / 注入字符各一类即可）。

## 去重规则（强制，本工具所有出用例步骤统一遵守）
- **同一（功能点 + 测试意图）只出一条用例**。不要把同一个验证点反复出。例如「下单 0 元」只一条，不要既出一条「现状：0 元能下单」又出一条「应有：0 元应拦截」。
- **禁止把"现状观察"和"应有预期"拆成两条**：本工具是分析型、没真跑系统，不存在"观察到的现状"。一个测试意图 = 一条用例：
  - 该测什么的步骤照常写进 `steps`；
  - **应有的判定结果写进 `expected`**（材料规定了照写，没规定就标 `ambiguity:true` 写「待澄清」，不二选一）；
  - 若需要引用材料原文作为预期依据，放进 `evidence`，**不要**因此另开一条用例。
- 跨步骤去重：本步（异常/边界）与 2_2（主流程）、2_4（权限/安全/状态机）若出现意图重叠，按性质归位、只在其归属步出一次（分工见上「分域清单·分工边界」）；2_5 会再做一轮全集去重，但各步自身先不要制造重复。

## 契约
遵循 meta.yaml 统一 `cases` 契约，本步不重抄。`type` 英文枚举：异常用例 `exception`，边界用例 `boundary`。

## 输出格式（合法 JSON，禁止任何前后说明文字）
```json
{
  "cases": [
    {
      "id": "TC-登录-101",
      "module": "用户登录",
      "fp_id": "FP-01",
      "title": "密码错误时提示且不登录",
      "priority": "P1",
      "type": "exception",
      "preconditions": "已注册手机号 13800138000、正确密码 Test1234，当前停在登录页",
      "steps": [
        "1、打开登录页面",
        "2、在「手机号」输入框输入 13800138000",
        "3、在「密码」输入框输入错误密码 wrong999",
        "4、点击「登录」按钮"
      ],
      "expected": "页面提示「手机号或密码错误」，停留在登录页，未登录成功",
      "ambiguity": false,
      "covers_upstream": [],
      "remark": "连续错误 N 次是否锁定、锁多久，材料未定义，见 Q-01，相关锁定用例预期待澄清",
      "automation_tag": "manual",
      "status": "designed",
      "evidence": "材料原文：『密码错误提示重新输入』"
    },
    {
      "id": "TC-注册-201",
      "module": "用户注册",
      "fp_id": "FP-03",
      "title": "昵称超过字段最大长度时被拦截",
      "priority": "P2",
      "type": "boundary",
      "preconditions": "当前在注册页（昵称最大长度材料未给，记为 <待澄清>）",
      "steps": [
        "1、打开注册页面",
        "2、在「昵称」输入框输入超过【昵称最大长度】+1 的字符串",
        "3、点击「注册」按钮"
      ],
      "expected": "材料未规定超长昵称的处理方式（截断 / 拦截提示），需澄清后定预期；见 Q-02",
      "ambiguity": true,
      "covers_upstream": ["HOLE-09"],
      "remark": "覆盖上游缺口 HOLE-09（昵称长度边界未定义）；字段最大长度待产品确认后回填具体数值",
      "automation_tag": "manual",
      "status": "designed",
      "evidence": "材料原文：『填写昵称完成注册』，未给昵称长度限制"
    }
  ],
  "summary": {"total": 0, "by_priority": {"P0": 0, "P1": 0, "P2": 0, "P3": 0}, "by_type": {"exception": 0, "boundary": 0}},
  "coverage": {"fp_covered": ["FP-01", "FP-03"], "boundary_left_ambiguous": ["FP-03 昵称长度"]},
  "domain_coverage": [
    {"domain": "D_ACCT|D_PAY|D_SUB|D_CONTENT|D_SEARCH|D_LIST|D_FILE|D_FORM|D_SOCIAL|D_MSG|D_AUTHZ|D_MKT|D_FLOW|D_PRIVACY|D_I18N|D_INTEG|D_GEN", "applicable": true, "applicability_basis": "<判定该域涉及的原文摘录,或『全文未见该域相关描述』>", "points_designed_here": ["支付结果分支:7种结果各一条", "幂等:重复回调不重复开通", "退款:部分退款"], "points_routed_to_2_4": ["风控盗刷", "订单状态机非法跳转"], "points_left_ambiguous": ["<PRD未定义、用例预期标待澄清的点+对应Q编号>"], "extra_cases_added": ["<清单未列、类比补出的用例;无则空>"]}
  ],
  "confidence": {"score": 0.0, "rationale": "..."}
}
```
