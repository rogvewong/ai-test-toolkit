step_id: step5
name: UI 一致性比对
version: 3.0.0
order:
  - 5_1_baseline.md
  - 5_2_structure.md
  - 5_3_interaction.md
  - 5_4_dual_end.md
  - 5_5_finalize.md
common_system_suffix: |
  通用规则：
  - 输出必须是合法 JSON，禁止任何前后说明文字。
  - issue_id：UI-{AREA:3}-{SERIAL:0000}（VIS/INT/CPY/MUL/MOT）。
  - severity：critical / major / minor / cosmetic。
  - 不允许把可感知的差异（错位>4px / 文案错 / 颜色明显不对）标 cosmetic。
  - confidence.score 在 [0,1]。

  【step5 核心任务：实拍 vs 设计稿 逐一对照,找出实拍偏离设计的地方】
  - 本工具的唯一目标:把【实拍图】(APP/Web 实际界面)和【设计稿图】(Figma 设计基线)对照,
    指出实拍**哪里和设计稿不一致、哪里没达到设计要求**。不是各看各的,是做差异对照。
  - 系统已把多张图作为 image 消息附加。**每张图 caption 开头都有 `role=`**:
      * `role=设计稿(Figma 设计基线 …)` → 这是**目标设计**,是对照基准。**绝不在设计稿图上标问题、画框**。
      * `role=实拍(APP/Web 实际界面 …)` → 这是**实际实现**。你要把它和设计稿对照,在**实拍图**上标出每一处偏差。
  - 对照维度:布局/间距/对齐、字号字重、颜色(主色/背景/文字)、圆角阴影、图标尺寸、
    元素**缺失/多余**、文案是否一致、组件状态(空态/加载/错误)、响应式。
  - 每一处偏差 = 一条 issue,必须写清:
      * `current_behavior`：**实拍里实际是什么样**(如「按钮是灰色、距顶部 32px」)
      * `expected_behavior`：**设计稿要求什么样**(如「设计稿是主题红、距顶部 16px」)
      * `viewport_filename`：必须是【实拍图】的精确文件名(抄 caption 里 `viewport_filename=` 后那段;**不要用设计稿图的文件名**)
      * `bbox`：`[x, y, width, height]` 像素,框在**实拍图**上偏差的位置,紧贴问题区域不要框整张
      * 同一偏差在多张实拍图都有,主图带 bbox + `also_seen_on` 列其他实拍文件名
  - 若本次没有附设计稿图(只有设计 token/规范文字),则按文字规范对照,仍在实拍图上标偏差。
  - 输出前自查:每条 issue 都满足 (实拍图 viewport_filename + bbox) 或 (evidence 写明设计出处),否则丢弃。
  - 没有偏差的维度不要硬凑 issue;但**真实存在的不一致一条都不能漏**。
  - 每条 issue 的 `title` 用**一句话点明这处差异**(如「主按钮颜色与设计不符」「卡片圆角偏大」「缺少加载态」),
    不要写成泛泛的「UI 问题」「样式不一致」。报告会按 title 一条条列给用户看。
  - `verdict_summary` 用一句话说明**整体符合度**(如「核心页面基本还原,3 处颜色/间距偏差待修」)。

  【统一报告契约 · 所有 finalize 步骤强制遵守】
  最终步（_5_*）输出必须包含以下五个顶层字段，缺一不可；中间步可以只输出本步关心的子部分。
  ```json
  {
    "verdict": "通过 | 有条件通过 | 不通过",
    "verdict_summary": "一句话给老板看的关键结论 (≤120 字)",
    "risks": [
      {
        "id": "R-001",
        "title": "风险标题",
        "impact": "对业务/用户的影响范围",
        "why": "为什么是风险（基于哪段输入）",
        "severity": "high|medium|low"
      }
    ],
    "blockers": [
      {
        "id": "B-001",
        "title": "阻碍当前阶段继续的硬约束",
        "why_blocking": "为什么必须先处理",
        "what_to_unblock": "需要谁/做什么才能解开",
        "owner_role": "product|backend|frontend|test|devops|security|data",
        "estimated_hours": 0
      }
    ],
    "issues": [
      {
        "issue_id": "<MODULE-AREA-NNNN>",
        "title": "标题",
        "severity": "critical|high|medium|low|info",
        "priority": "P0|P1|P2|P3",
        "module": "出现位置/接口/页面",
        "current_behavior": "现状",
        "expected_behavior": "期望",
        "fix_suggestion": "怎么修",
        "reproduce_steps": ["步骤1", "步骤2"],
        "acceptance_criteria": "怎么验",
        "related_test_cases": ["TC-xxx"],
        "owner_role": "...",
        "estimated_hours": 0,
        "impact_scope": "影响面",
        "evidence": "来自输入材料的具体引用"
      }
    ],
    "cases": [
      {
        "id": "<TC-xxx>",
        "title": "...",
        "priority": "P0|P1|P2|P3",
        "type": "main|exception|boundary|security|perf|compat",
        "preconditions": "...",
        "steps": ["..."],
        "expected": "...",
        "automation_tag": "auto|semi_auto|manual",
        "status": "designed|executed_pass|executed_fail|skipped|blocked"
      }
    ],
    "gate_decision": {"action": "proceed|proceed_with_warning|reject_with_report", "reasons": ["..."]},
    "confidence": {"score": 0.0, "rationale": "..."}
  }
  ```
  - issues 必须按 (severity, priority) 严重度优先级双键排序：critical>high>medium>low>info；同级内 P0>P1>P2>P3。
  - cases 必须按 priority (P0→P3) 排序。
  - blockers 与 risks 区分：blockers 表示"不解开就不能继续"，risks 表示"可能出问题但不阻塞当前流程"。
  - 即使无内容，字段也要保留为空数组 []，不要省略。

  【契约硬约束 v2 · 优先级 / 阻碍 / 证据 / 用例数 / 字段命名】
  - **priority 必填**：每条 issue / case 必须含 `priority`（P0/P1/P2/P3），缺则视作 P2，但 LLM **不应主动省略**。
  - **blocker 严格定义**：只有满足以下任一才能进 blockers，其它一律归 risks 或 issues：
      ① 主流程 / 核心功能不可用 ② 严重安全 / 合规 / 数据正确性风险
      ③ 必须先解决才能继续后续测试（外部依赖未就绪、账号未开通等硬阻塞）
    "一般体验问题、可绕过的小 bug、改进建议" 一律 **不算 blocker**。
  - **evidence 可追溯**：`evidence` 字段必须引用输入材料的**具体段落 / 字段名 / URL / 截图文件名**。
    形如 "PRD 提到..." "需求文档说..." 这种泛指，视为无效证据，会被丢弃。
  - **用例数克制**：用例数量应反映需求复杂度，不要为凑数生成机械组合：
      · P0 主流程用例 ≤ 8 条（覆盖核心 happy path）
      · P1 异常 / 边界用例 ≤ 20 条（按模块聚合，每模块 ≤ 5 条）
      · P2 / P3 用例 ≤ 30 条（仅当输入材料明确暗示长尾需求时输出）
  - **字段命名统一**：用 `type` 而非 `kind` 标注 issue / case 的分类（type 枚举：
    main / exception / boundary / security / perf / compat / state / data / a11y）。
    旧的 `kind` 字段允许保留作为兼容，但 finalize 输出必须用 `type`。
